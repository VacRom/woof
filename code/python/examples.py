# Assignment commands
x = 2
y = 1

# Conditional. Takes a variable expression uses a function to compare
# it with a literal expression. We check our state (for variable x) to
# find which ite branch to take.

if (x == 2 or y == x):
    x = 0
    print(x)
else:
    x = 1
    print(x)

# Iteration

# Procedures and procedure call
def fun(a, b, c, x)
    result = a * (x * x) + b * (x) + c
    return result
